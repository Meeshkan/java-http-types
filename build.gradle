apply plugin: 'java'

repositories {
    jcenter()
}

sourceCompatibility = 1.8
version = "0.1.0"

dependencies {
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter', version: '5.5.2'
}

test {
    useJUnitPlatform()
}
